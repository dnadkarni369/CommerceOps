apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: commerceops
  labels:
    app: order-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8002"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: order-service
          # Replace with your ECR image URI after Jenkins pushes a build, e.g.:
          # <account_id>.dkr.ecr.<region>.amazonaws.com/commerceops/order-service:latest
          image: commerceops/order-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8002
          envFrom:
            - configMapRef:
                name: commerceops-config
            - secretRef:
                name: commerceops-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8002
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: commerceops
spec:
  selector:
    app: order-service
  ports:
    - port: 8002
      targetPort: 8002
