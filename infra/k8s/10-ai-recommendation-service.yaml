apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-recommendation-service
  namespace: commerceops
  labels:
    app: ai-recommendation-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-recommendation-service
  template:
    metadata:
      labels:
        app: ai-recommendation-service
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8003"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: ai-recommendation-service
          # Replace with your ECR image URI after Jenkins pushes a build, e.g.:
          # <account_id>.dkr.ecr.<region>.amazonaws.com/commerceops/ai-recommendation-service:latest
          image: commerceops/ai-recommendation-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: commerceops-config
            - secretRef:
                name: commerceops-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: ai-recommendation-service
  namespace: commerceops
spec:
  selector:
    app: ai-recommendation-service
  ports:
    - port: 8003
      targetPort: 8003
